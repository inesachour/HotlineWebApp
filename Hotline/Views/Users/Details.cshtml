@model Hotline.Models.User

@{
    ViewData["Title"] = "Details";
}

<div>

    <div class="row p-0 border-0 mt-5" id="center">
        <div class="col-sm-4 border p-0">
            <button class="btn btn-light btn-block mb-0 rounded-0" style="height:15vh" disabled>Compte</button>
            <a class="btn btn-primary btn-block mt-0 mb-0 rounded-0 d-flex align-items-center justify-content-center" style="height:15vh" asp-action="Edit" asp-route-id="@Model.Id">
                Modifier Compte
            </a>
            <a class="btn btn-primary btn-block mt-0 rounded-0 d-flex align-items-center justify-content-center" style="height:15vh" asp-action="EditPassword" asp-route-id="@Model.Id">
                Modifier mot de passe
            </a>
        </div>
        <div class="col-sm-7 border pl-0">
            <div class="mt-5 pl-0 ml-4">
                <!--<h3 class="text-muted">@Html.DisplayNameFor(model => model.Login) :</h3>
    <h6 class="mb-4 ml-2 ">@Html.DisplayFor(model => model.Login)</h6>
    <h3 class="text-muted mt-2">@Html.DisplayNameFor(model => model.Admin) : </h3>
    <p>
        @if (Model.Admin == true)
        {<h4>&#10004;</h4>}
        else
        {<h4>&#10006;</h4>}
    </p> -->
                <form class="mt-2">
                    <div asp-validation-summary="ModelOnly" class="text-danger"></div>
                    <h3 class="text-center mb-3">Compte</h3>
                    <div class="form-group">
                        <label asp-for="Login" class="control-label"></label>
                        <input asp-for="Login" class="form-control" disabled />
                    </div>
                    <div class="form-group">
                        <label asp-for="Admin" class="control-label"></label>
                        @if (Model.Admin == true)
                        {<h2 class="text-center">&#10004;</h2>}
                        else
                        {<h2 class="text-center">&#10006;</h2>}
                    </div>

                </form>
            </div>
        </div>
        <div>
        </div>
    </div>

</div>
